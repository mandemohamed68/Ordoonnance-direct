import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ordonnance_direct.app',
  appName: 'OrdonnanceDirectApp',
  webDir: 'dist'
};

export default config;
